/**
 * Drizzle schema — Warform Commander.
 *
 * Intentionally empty for now (connection wired ahead of the schema).
 *
 * The first tables — battle results / stored replays — land with Feature 1's
 * finalized replay + result format (see `specs/001-battle-sim-core/spec.md` and
 * `reference/warformcommander-firstpass-stats.md`). Accounts, saved squads,
 * defense snapshots, and net-victory ranking follow with Feature 7.
 */

export {};

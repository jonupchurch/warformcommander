# Balance reports

Default output directory for the auto-balancer (`crates/balancer`). Generated
reports (`balance-report.json` + `balance-report.md`) land here; commit the ones
worth keeping alongside the balance table they evaluated.
